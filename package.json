{
  "private": true,
  "scripts": {
    "start": "eleventy --serve --quiet",
    "build": "rm -rf dist && eleventy",
    "deploy": "cd dist && rsync --archive --compress --delete --exclude=.DS_Store . pepelsbey.dev:/var/www/pepelsbey.dev/html/"
  },
  "engines": {
    "node": "16",
    "npm": "8"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0-canary.12",
    "autoprefixer": "^10.4.7",
    "esbuild": "^0.14.43",
    "html-minifier": "^4.0.0",
    "js-yaml": "^4.1.0",
    "postcss": "^8.4.13",
    "postcss-csso": "^6.0.0",
    "postcss-import": "^14.1.0",
    "pretty-data": "^0.40.0"
  }
}
