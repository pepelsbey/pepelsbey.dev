---
layout: page.njk
---

{% include 'header.njk' %}

<div class="main">
    <div class="main__gaps">
        <main class="main__wrapper">
            <header class="heading">
                <h1 class="heading__title">
                    {{ title }}
                </h1>
            </header>
            <div class="content content-block content-inline">
                {{ content | safe }}
            </div>
        </main>
        <aside class="main__wrapper">
            <header class="heading">
                <h2 class="heading__title">
                    Articles
                </h2>
                <span class="heading__number">
                    {{ collections.articles.length }}
                </span>
                <a class="heading__link action action--hover" href="/articles/">
                    <svg class="action__icon" width="24" height="24" aria-hidden="true">
                        <use href="/images/icons.svg#right"></use>
                    </svg>
                </a>
            </header>

            {% from 'article-card-list.njk' import articleCardList with context %}

            {{ articleCardList(
                collection=collections.articles,
                limit=4
            ) }}
        </aside>
    </div>
</div>

{% include 'footer.njk' %}
