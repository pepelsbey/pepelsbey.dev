---
layout: page.njk
---

{% include 'header.njk' %}

<div class="main">
    <div class="main__wrapper main__gaps">
        <main>
            <header class="heading">
                <h1 class="heading__title heading__title--top">
                    {{ title }}
                </h1>
            </header>
            <div class="content content-block content-inline">
                {{ content | safe }}
            </div>
        </main>
        <aside>
            <header class="heading">
                <h2 class="heading__title">
                    Articles
                </h2>
                <span class="heading__number">
                    {{ collections.articles.length }}
                </span>
                <a class="heading__link action action--hover" href="/articles/" aria-label="View all articles">
                    <span class="heading__label">
                        View all
                    </span>
                    <svg class="action__icon" width="24" height="24" aria-hidden="true">
                        <use href="/images/icons.svg#right"></use>
                    </svg>
                </a>
            </header>

            {% from 'article-card-list.njk' import articleCardList with context %}

            {{ articleCardList(
                collection=collections.articles,
                limit=4
            ) }}
        </aside>
    </div>
</div>

{% include 'footer.njk' %}
